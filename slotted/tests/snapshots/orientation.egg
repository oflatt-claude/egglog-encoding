;;; COMPILED from slotted/tests/orientation.egg by slotted/slotted-egglog.py.
;;;
;;; Only the forms THIS file contributes. The machinery for its 1 constructors,
;;; and anything an included library brought, are snapshotted by the generators
;;; that emit them -- committing them again per test would be the same thousands
;;; of lines over and over.

(include "slotted/tests/slotted-egraph-encoding-11.egg")

(let $y (App2 "y" (map-of 0 1) (Var 0) (map-of 0 2) (Var 0)))
(let $l (App2 "l" (map-of 0 3) (Var 0) (map-of 0 4) (Var 0)))
(let $f (App2 "f" (map-of 0 5) (Var 0) (map-of 0 6) (Var 0)))
(check (= (ordering-max $l $y) $l))
(check (= (ordering-max $f $l) $f))
(Equated $f (map-of 3 5 4 6) $l)
(union $f $y)
(run-schedule (saturate (run slotted)))
(relation Backwards (U Renaming U))
(ruleset orientcheck)
(rule ((RenamesToLeader fo mo lo) (!= fo lo) (= lo (ordering-max fo lo))) ((Backwards fo mo lo)) :ruleset orientcheck)
(run-schedule (run orientcheck))
(fail (check (Backwards fo mo lo)))
(check (RenamesToLeader $f mf leader) (RenamesToLeader $l ml leader))

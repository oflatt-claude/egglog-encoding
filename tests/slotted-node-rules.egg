;;; GENERATED by slotted-experiments/gen-node-rules.py -- do not edit.
;;;
;;; One block per constructor. A CHILD column occupies `Renaming U` and
;;; contributes its slots; a payload column is one column and contributes
;;; none, so a zero-child constructor is just a payload leaf.

;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;;; App2 :: String child child
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;

(constructor App2 (String Renaming U Renaming U) U)

;; every class holding a node has a self-loop, so a query can reach it
(rule ((= e1 (App2 p1 m1 c1 m2 c2))
       (= m (map-union (map-image m1) (map-image m2))))
      ((RenamesToLeader e1 m e1)))

;; alpha-finder: two nodes equal up to renaming, one eliminated
(rule ((= e1 (App2 p1 m1_o c1 m2_o c2))
       (= e2 (App2 p1 b1 c1 b2 c2))
       (= e1 (ordering-max e1 e2))
       (RenamesToLeader c1 sym1 c1)
       (RenamesToLeader c2 sym2 c2)
       (= m1 (compose m1_o sym1))
       (= m2 (compose m2_o sym2))
       (= m (find-mapping m1 m2 b1 b2))
       (guard
         (or (bool-!= e1 e2)
             (and (bool= e1 e2)
                  (or (and (bool= (ordering-max m1 b1) b1) (bool-!= m1 b1))
              (and (bool= m1 b1)
                   (and (bool= (ordering-max m2 b2) b2) (bool-!= m2 b2))))))))
      ((RenamesToLeader e1 m e2)
       (delete (App2 p1 m1 c1 m2 c2))))

;; the same solve kept as a symmetry, non-destructively
(rule ((= e (App2 p1 m1_o c1 m2_o c2))
       (RenamesToLeader c1 sym1 c1)
       (RenamesToLeader c2 sym2 c2)
       (= m1 (compose m1_o sym1))
       (= m2 (compose m2_o sym2))
       (= sym_out (find-mapping m1_o m2_o m1 m2)))
      ((RenamesToLeader e sym_out e)))

;; migration: move a follower's node into the leader's frame
(rule ((RenamesToLeader e2 m e1)
       (= e2 (App2 p1 m1 c1 m2 c2))
       (!= e1 e2)
       ; narrowing would understate the child's slots, so decline instead
       (= n1 (compose-total (inverse m) m1))
       (= n2 (compose-total (inverse m) m2)))
      ((union e1 (App2 p1 n1 c1 n2 c2))
       (delete (App2 p1 m1 c1 m2 c2))))

;; child-update, child 1
(rule ((RenamesToLeader c1 m c')
       (= node (App2 p1 m1 c1 m2 c2))
       ; if the class is unchanged then m must be idempotent: no self-symmetries
       (guard (or (bool-!= c1 c') (bool= (compose m m) m)))
       ; and the new node must differ from the old one
       (guard (or (bool-!= c1 c')
                  (bool-!= (compose m1 m) m1))))
      ((union node (App2 p1 (compose m1 m) c' m2 c2))
       (delete (App2 p1 m1 c1 m2 c2))))

;; child-update, child 2
(rule ((RenamesToLeader c2 m c')
       (= node (App2 p1 m1 c1 m2 c2))
       ; if the class is unchanged then m must be idempotent: no self-symmetries
       (guard (or (bool-!= c2 c') (bool= (compose m m) m)))
       ; and the new node must differ from the old one
       (guard (or (bool-!= c2 c')
                  (bool-!= (compose m2 m) m2))))
      ((union node (App2 p1 m1 c1 (compose m2 m) c'))
       (delete (App2 p1 m1 c1 m2 c2))))

;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;;; App3 :: String child child child
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;

(constructor App3 (String Renaming U Renaming U Renaming U) U)

;; every class holding a node has a self-loop, so a query can reach it
(rule ((= e1 (App3 p1 m1 c1 m2 c2 m3 c3))
       (= m (map-union (map-image m1) (map-union (map-image m2) (map-image m3)))))
      ((RenamesToLeader e1 m e1)))

;; alpha-finder: two nodes equal up to renaming, one eliminated
(rule ((= e1 (App3 p1 m1_o c1 m2_o c2 m3_o c3))
       (= e2 (App3 p1 b1 c1 b2 c2 b3 c3))
       (= e1 (ordering-max e1 e2))
       (RenamesToLeader c1 sym1 c1)
       (RenamesToLeader c2 sym2 c2)
       (RenamesToLeader c3 sym3 c3)
       (= m1 (compose m1_o sym1))
       (= m2 (compose m2_o sym2))
       (= m3 (compose m3_o sym3))
       (= m (find-mapping m1 m2 m3 b1 b2 b3))
       (guard
         (or (bool-!= e1 e2)
             (and (bool= e1 e2)
                  (or (and (bool= (ordering-max m1 b1) b1) (bool-!= m1 b1))
              (and (bool= m1 b1)
                   (or (and (bool= (ordering-max m2 b2) b2) (bool-!= m2 b2))
              (and (bool= m2 b2)
                   (and (bool= (ordering-max m3 b3) b3) (bool-!= m3 b3))))))))))
      ((RenamesToLeader e1 m e2)
       (delete (App3 p1 m1 c1 m2 c2 m3 c3))))

;; the same solve kept as a symmetry, non-destructively
(rule ((= e (App3 p1 m1_o c1 m2_o c2 m3_o c3))
       (RenamesToLeader c1 sym1 c1)
       (RenamesToLeader c2 sym2 c2)
       (RenamesToLeader c3 sym3 c3)
       (= m1 (compose m1_o sym1))
       (= m2 (compose m2_o sym2))
       (= m3 (compose m3_o sym3))
       (= sym_out (find-mapping m1_o m2_o m3_o m1 m2 m3)))
      ((RenamesToLeader e sym_out e)))

;; migration: move a follower's node into the leader's frame
(rule ((RenamesToLeader e2 m e1)
       (= e2 (App3 p1 m1 c1 m2 c2 m3 c3))
       (!= e1 e2)
       ; narrowing would understate the child's slots, so decline instead
       (= n1 (compose-total (inverse m) m1))
       (= n2 (compose-total (inverse m) m2))
       (= n3 (compose-total (inverse m) m3)))
      ((union e1 (App3 p1 n1 c1 n2 c2 n3 c3))
       (delete (App3 p1 m1 c1 m2 c2 m3 c3))))

;; child-update, child 1
(rule ((RenamesToLeader c1 m c')
       (= node (App3 p1 m1 c1 m2 c2 m3 c3))
       ; if the class is unchanged then m must be idempotent: no self-symmetries
       (guard (or (bool-!= c1 c') (bool= (compose m m) m)))
       ; and the new node must differ from the old one
       (guard (or (bool-!= c1 c')
                  (bool-!= (compose m1 m) m1))))
      ((union node (App3 p1 (compose m1 m) c' m2 c2 m3 c3))
       (delete (App3 p1 m1 c1 m2 c2 m3 c3))))

;; child-update, child 2
(rule ((RenamesToLeader c2 m c')
       (= node (App3 p1 m1 c1 m2 c2 m3 c3))
       ; if the class is unchanged then m must be idempotent: no self-symmetries
       (guard (or (bool-!= c2 c') (bool= (compose m m) m)))
       ; and the new node must differ from the old one
       (guard (or (bool-!= c2 c')
                  (bool-!= (compose m2 m) m2))))
      ((union node (App3 p1 m1 c1 (compose m2 m) c' m3 c3))
       (delete (App3 p1 m1 c1 m2 c2 m3 c3))))

;; child-update, child 3
(rule ((RenamesToLeader c3 m c')
       (= node (App3 p1 m1 c1 m2 c2 m3 c3))
       ; if the class is unchanged then m must be idempotent: no self-symmetries
       (guard (or (bool-!= c3 c') (bool= (compose m m) m)))
       ; and the new node must differ from the old one
       (guard (or (bool-!= c3 c')
                  (bool-!= (compose m3 m) m3))))
      ((union node (App3 p1 m1 c1 m2 c2 (compose m3 m) c'))
       (delete (App3 p1 m1 c1 m2 c2 m3 c3))))

;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;;; App4 :: String child child child child
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;

(constructor App4 (String Renaming U Renaming U Renaming U Renaming U) U)

;; every class holding a node has a self-loop, so a query can reach it
(rule ((= e1 (App4 p1 m1 c1 m2 c2 m3 c3 m4 c4))
       (= m (map-union (map-image m1) (map-union (map-image m2) (map-union (map-image m3) (map-image m4))))))
      ((RenamesToLeader e1 m e1)))

;; alpha-finder: two nodes equal up to renaming, one eliminated
(rule ((= e1 (App4 p1 m1_o c1 m2_o c2 m3_o c3 m4_o c4))
       (= e2 (App4 p1 b1 c1 b2 c2 b3 c3 b4 c4))
       (= e1 (ordering-max e1 e2))
       (RenamesToLeader c1 sym1 c1)
       (RenamesToLeader c2 sym2 c2)
       (RenamesToLeader c3 sym3 c3)
       (RenamesToLeader c4 sym4 c4)
       (= m1 (compose m1_o sym1))
       (= m2 (compose m2_o sym2))
       (= m3 (compose m3_o sym3))
       (= m4 (compose m4_o sym4))
       (= m (find-mapping m1 m2 m3 m4 b1 b2 b3 b4))
       (guard
         (or (bool-!= e1 e2)
             (and (bool= e1 e2)
                  (or (and (bool= (ordering-max m1 b1) b1) (bool-!= m1 b1))
              (and (bool= m1 b1)
                   (or (and (bool= (ordering-max m2 b2) b2) (bool-!= m2 b2))
              (and (bool= m2 b2)
                   (or (and (bool= (ordering-max m3 b3) b3) (bool-!= m3 b3))
              (and (bool= m3 b3)
                   (and (bool= (ordering-max m4 b4) b4) (bool-!= m4 b4))))))))))))
      ((RenamesToLeader e1 m e2)
       (delete (App4 p1 m1 c1 m2 c2 m3 c3 m4 c4))))

;; the same solve kept as a symmetry, non-destructively
(rule ((= e (App4 p1 m1_o c1 m2_o c2 m3_o c3 m4_o c4))
       (RenamesToLeader c1 sym1 c1)
       (RenamesToLeader c2 sym2 c2)
       (RenamesToLeader c3 sym3 c3)
       (RenamesToLeader c4 sym4 c4)
       (= m1 (compose m1_o sym1))
       (= m2 (compose m2_o sym2))
       (= m3 (compose m3_o sym3))
       (= m4 (compose m4_o sym4))
       (= sym_out (find-mapping m1_o m2_o m3_o m4_o m1 m2 m3 m4)))
      ((RenamesToLeader e sym_out e)))

;; migration: move a follower's node into the leader's frame
(rule ((RenamesToLeader e2 m e1)
       (= e2 (App4 p1 m1 c1 m2 c2 m3 c3 m4 c4))
       (!= e1 e2)
       ; narrowing would understate the child's slots, so decline instead
       (= n1 (compose-total (inverse m) m1))
       (= n2 (compose-total (inverse m) m2))
       (= n3 (compose-total (inverse m) m3))
       (= n4 (compose-total (inverse m) m4)))
      ((union e1 (App4 p1 n1 c1 n2 c2 n3 c3 n4 c4))
       (delete (App4 p1 m1 c1 m2 c2 m3 c3 m4 c4))))

;; child-update, child 1
(rule ((RenamesToLeader c1 m c')
       (= node (App4 p1 m1 c1 m2 c2 m3 c3 m4 c4))
       ; if the class is unchanged then m must be idempotent: no self-symmetries
       (guard (or (bool-!= c1 c') (bool= (compose m m) m)))
       ; and the new node must differ from the old one
       (guard (or (bool-!= c1 c')
                  (bool-!= (compose m1 m) m1))))
      ((union node (App4 p1 (compose m1 m) c' m2 c2 m3 c3 m4 c4))
       (delete (App4 p1 m1 c1 m2 c2 m3 c3 m4 c4))))

;; child-update, child 2
(rule ((RenamesToLeader c2 m c')
       (= node (App4 p1 m1 c1 m2 c2 m3 c3 m4 c4))
       ; if the class is unchanged then m must be idempotent: no self-symmetries
       (guard (or (bool-!= c2 c') (bool= (compose m m) m)))
       ; and the new node must differ from the old one
       (guard (or (bool-!= c2 c')
                  (bool-!= (compose m2 m) m2))))
      ((union node (App4 p1 m1 c1 (compose m2 m) c' m3 c3 m4 c4))
       (delete (App4 p1 m1 c1 m2 c2 m3 c3 m4 c4))))

;; child-update, child 3
(rule ((RenamesToLeader c3 m c')
       (= node (App4 p1 m1 c1 m2 c2 m3 c3 m4 c4))
       ; if the class is unchanged then m must be idempotent: no self-symmetries
       (guard (or (bool-!= c3 c') (bool= (compose m m) m)))
       ; and the new node must differ from the old one
       (guard (or (bool-!= c3 c')
                  (bool-!= (compose m3 m) m3))))
      ((union node (App4 p1 m1 c1 m2 c2 (compose m3 m) c' m4 c4))
       (delete (App4 p1 m1 c1 m2 c2 m3 c3 m4 c4))))

;; child-update, child 4
(rule ((RenamesToLeader c4 m c')
       (= node (App4 p1 m1 c1 m2 c2 m3 c3 m4 c4))
       ; if the class is unchanged then m must be idempotent: no self-symmetries
       (guard (or (bool-!= c4 c') (bool= (compose m m) m)))
       ; and the new node must differ from the old one
       (guard (or (bool-!= c4 c')
                  (bool-!= (compose m4 m) m4))))
      ((union node (App4 p1 m1 c1 m2 c2 m3 c3 (compose m4 m) c'))
       (delete (App4 p1 m1 c1 m2 c2 m3 c3 m4 c4))))

;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;;; Num :: i64
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;

(constructor Num (i64) U)

;; every class holding a node has a self-loop, so a query can reach it
(rule ((= e1 (Num p1))
       (= m (map-empty)))
      ((RenamesToLeader e1 m e1)))

;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;;; Sym :: String
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;

(constructor Sym (String) U)

;; every class holding a node has a self-loop, so a query can reach it
(rule ((= e1 (Sym p1))
       (= m (map-empty)))
      ((RenamesToLeader e1 m e1)))

;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;;; Scale :: i64 child
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;

(constructor Scale (i64 Renaming U) U)

;; every class holding a node has a self-loop, so a query can reach it
(rule ((= e1 (Scale p1 m1 c1))
       (= m (map-image m1)))
      ((RenamesToLeader e1 m e1)))

;; alpha-finder: two nodes equal up to renaming, one eliminated
(rule ((= e1 (Scale p1 m1_o c1))
       (= e2 (Scale p1 b1 c1))
       (= e1 (ordering-max e1 e2))
       (RenamesToLeader c1 sym1 c1)
       (= m1 (compose m1_o sym1))
       (= m (find-mapping m1 b1))
       (guard
         (or (bool-!= e1 e2)
             (and (bool= e1 e2)
                  (and (bool= (ordering-max m1 b1) b1) (bool-!= m1 b1))))))
      ((RenamesToLeader e1 m e2)
       (delete (Scale p1 m1 c1))))

;; the same solve kept as a symmetry, non-destructively
(rule ((= e (Scale p1 m1_o c1))
       (RenamesToLeader c1 sym1 c1)
       (= m1 (compose m1_o sym1))
       (= sym_out (find-mapping m1_o m1)))
      ((RenamesToLeader e sym_out e)))

;; migration: move a follower's node into the leader's frame
(rule ((RenamesToLeader e2 m e1)
       (= e2 (Scale p1 m1 c1))
       (!= e1 e2)
       ; narrowing would understate the child's slots, so decline instead
       (= n1 (compose-total (inverse m) m1)))
      ((union e1 (Scale p1 n1 c1))
       (delete (Scale p1 m1 c1))))

;; child-update, child 1
(rule ((RenamesToLeader c1 m c')
       (= node (Scale p1 m1 c1))
       ; if the class is unchanged then m must be idempotent: no self-symmetries
       (guard (or (bool-!= c1 c') (bool= (compose m m) m)))
       ; and the new node must differ from the old one
       (guard (or (bool-!= c1 c')
                  (bool-!= (compose m1 m) m1))))
      ((union node (Scale p1 (compose m1 m) c'))
       (delete (Scale p1 m1 c1))))

;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;;; binders
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;

;; `lambda` binds its first child's slot
(rule ((RenamesToLeader (App2 "lambda" mvar (Var 0) m2 c2) ml l)
       (= v (map-get mvar 0)))
      ((RenamesToLeader (App2 "lambda" mvar (Var 0) m2 c2) (inverse (map-remove (inverse ml) v)) l)))

;; `let` binds its first child's slot
(rule ((RenamesToLeader (App3 "let" mvar (Var 0) m2 c2 m3 c3) ml l)
       (= v (map-get mvar 0)))
      ((RenamesToLeader (App3 "let" mvar (Var 0) m2 c2 m3 c3) (inverse (map-remove (inverse ml) v)) l)))
